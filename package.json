{
  "name": "smart-healthcare-queue-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd shared && npm install && cd ../backend && npm install && npm run build && cd ../frontend && npm install && npm run build",
    "start": "cd backend && npm start"
  }
}
